rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /analyses/{uid}/results/{doc} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
